# Skill Format

Skills use `SKILL.md` with YAML frontmatter:

```yaml
---
name: skill-name
description: One sentence with trigger phrases for when to use
---
```

- Name: lowercase-with-hyphens
- Body: markdown with numbered steps
- Each step independently actionable
- Include full code blocks for files to create
- Include chmod commands for scripts
